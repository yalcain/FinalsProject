<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Add this for reading images (for maintenance upload) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalsActivity"
        tools:targetApi="31">

        <!-- LAUNCHER SCREEN -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AUTH -->
        <activity android:name=".LoginActivity" android:exported="false" />
        <activity android:name=".RegisterActivity" android:exported="false" />

        <!-- TENANT SCREENS -->
        <activity android:name=".TenantDashboard" android:exported="false" /> <!-- ✅ You had wrong name before -->
        <activity android:name=".TenantsProfileActivity" android:exported="false" />
        <activity android:name=".UserPayment" android:exported="false" />
        <activity android:name=".PaymentActivity" android:exported="false" />
        <activity android:name=".ReceiptActivity" android:exported="false" />
        <activity android:name=".NotificationActivity" android:exported="false" />
        <activity android:name=".Maintenance" android:exported="false" />
        <activity android:name=".BookingActivity" android:exported="false" />
        <activity android:name=".DormDetailsActivity" android:exported="false" />
        <activity android:name=".homeActivity" android:exported="false" />
        <activity android:name=".SdDormActivity" android:exported="false" />
        <activity android:name=".Loft22Activity" android:exported="false" />
        <activity android:name=".MilfloresBHActivity" android:exported="false" />
        <activity android:name=".TheDormitoryActivity" android:exported="false" />
        <activity android:name=".Reservation" android:exported="false" />

        <!-- ADMIN SCREENS -->
        <activity android:name=".AdminDashboard" android:exported="false" />
        <activity android:name=".AdminPaymentActivity" android:exported="false" /> <!-- ✅ FIXED NAME (was wrong) -->
        <activity android:name=".AdminPostActivity" android:exported="false" />
        <activity android:name=".ManageUsersActivity" android:exported="false" />
        <activity android:name=".ManageRoomsActivity" android:exported="false" />
        <activity android:name=".PostAnnouncementActivity" android:exported="false" />
        <activity android:name=".AddPlaceActivity" android:exported="false" />

        <!-- SETTINGS -->
        <activity android:name=".SettingsActivity" android:exported="false" />

        <!-- ❌ REMOVED: VerificationActivity (you deleted this file!) -->
        <!-- ❌ REMOVED: AdminPayment / AdminPaymentView (duplicate/wrong names) -->
        <!-- ❌ REMOVED: AccountActivity (if you don't have this file) -->

    </application>

</manifest>